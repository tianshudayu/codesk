# GitHub Upload Notes

This export contains source files only.

Removed from export:
- local logs, databases, virtualenv, IDE state, build outputs, caches
- deployment helper scripts with real endpoints or credentials
- generated binaries and temporary screenshots

Adjusted in export:
- README deployment section replaced with a generic note
- clients/windows/codesk-client.json uses https://your-cloud-host
- tests/test_cloud_gateway.py uses a stale-host.example placeholder for the stale claim URL test
